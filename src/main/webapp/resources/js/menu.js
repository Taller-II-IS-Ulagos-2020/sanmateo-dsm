jQuery(document).ready(function() {
		
		$("#idICliente").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idACliente").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idECliente").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idLCliente").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idIProducto").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idEProducto").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idAProducto").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idLProducto").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idCProducto").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idProductoProveedor").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		
		
		$("#idIProveedor").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idEProveedor").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idAProveedor").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idLProveedor").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		$("#idCProducto").click(function(e) {
			
			e.preventDefault();
			$("#someFrame").attr("src", $(this).attr("href"));
		});
		
		
});